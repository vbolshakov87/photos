<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">×</span><span class="sr-only">Close</span></button>
    <h4 class="modal-title" id="myLargeModalLabel">Title</h4>
</div>
<div class="modal-body">
  <div class="fotorama"
       data-width="100%"
       data-ratio="3/2"
       data-maxheight="77%"
       data-nav="thumbs"
       data-transition="dissolve"
       data-loop="true"
       data-keyboard="true"
       data-allowfullscreen="true"
       data-nav="thumbs"
       data-thumbheight="64"
       data-click="true"
       data-swipe="true"
       data-hash="true">
  <%@photos.each do |photo|%>
    <a href="<%=photo.image.url(:blog)%>"><%= image_tag photo.image.url(:thumb), size: photo.image.image_size(:thumb) %></a>
  <%end%>
  </div>
</div>